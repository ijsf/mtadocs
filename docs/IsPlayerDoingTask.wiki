
{{Client function}}
{{Deprecated}}

Please use [[/isPedDoingTask.md|isPedDoingTask]]

This function checks if the specified player is carrying out a certain [[/List_of_player_tasks.md|task]].

==Syntax==
<syntaxhighlight lang="lua">bool isPlayerDoingTask ( player thePlayer, string taskName )</syntaxhighlight>

===Required Arguments===
* '''thePlayer''': A [[/player.md|player]] object referencing the specified player.
* '''taskName''': A string containing the name of the [[/List_of_player_tasks.md|task]] you're checking for.

===Returns===
Returns ''true'' if the player is currently doing the task, ''false'' otherwise.

==Example==
This example checks if the player who entered the 'doingdriveby' command is doing a drive-by (clientside).
<syntaxhighlight lang="lua">
function amIDoingADriveby ()
  if ( isPlayerDoingTask ( getLocalPlayer(), "TASK_SIMPLE_GANG_DRIVEBY" ) ) then
    outputChatBox ( getPlayerName ( getLocalPlayer() ) .. " is doing a driveby!!!" )
  else
    outputChatBox ( getPlayerName ( getLocalPlayer() ) .. " is not doing a driveby" )
  end
end
addCommandHandler ( "doingdriveby", amIDoingADriveby )
</syntaxhighlight>

==See Also==
{{Client player functions}}