{{Useless Function}}
<lowercasetitle/>


This function checks if element is team.

==Syntax==
<syntaxhighlight lang="lua"> bool isElementTeam( element theElement ) </syntaxhighlight>

===Returns===
Returns true if the element is team, otherwise false.

===Code===
<syntaxhighlight lang="lua">

function isElementTeam(theTeam)
   if isElement(theTeam) and (getElementType ( theTeam ) == "team") then
		return true
   end	
	return false
end


</syntaxhighlight>

===Example===
<section name="Client" class="client" show="true">
<syntaxhighlight lang="lua"></syntaxhighlight>
</section>


Author: Booo 

==See Also==
{{Useful_Functions}}